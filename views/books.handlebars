<h1>Livros Disponíveis></h1>

<div class="books-container">
    {{#each books}}
        <div>
            <p>#{{this.id}} - 
            <span class="bold"><a href="/books/{{this.id}}">{{this.title}}</a></span> - {{this.pages}} páginas</p>
        </div>
    {{/each}}
</div>